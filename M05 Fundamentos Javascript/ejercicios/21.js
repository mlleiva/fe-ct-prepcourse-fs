function suma(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su suma.
  // Tu código:
  let resultado = x + y
  return(resultado)
}
suma(10,8)
module.exports = suma;